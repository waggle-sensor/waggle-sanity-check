#!/bin/bash
echo "RPI K3S Node Test:"

k3snodes=$(kubectl get nodes --request-timeout='5s')
rpiNodeSub=$(printf '.ws-rpi      Ready')

if [[ "$k3snodes" == *"$rpiNodeSub"* ]]; then
    echo "RPI Configured and Ready as k3s Agent"
    exit 0
fi

echo "No RPI configured as K3S agent"
exit 1
